{
  "private": true,
  "name": "LinkediNTU",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "install-all": "yarn install",
    "build-all": "yarn workspaces run build",
    "lint-all": "yarn workspaces run lint"
  }
}

